SANITY_STUDIO_PROJECT_ID=
SANITY_STUDIO_DATASET=

# Optional. Overrides the recipient of the "Send Test Email" document action.
# Useful when your sender domain is Resend's onboarding@resend.dev, which can
# only deliver to the email that owns the Resend account.
# SANITY_STUDIO_TEST_RECIPIENT=you@example.com
